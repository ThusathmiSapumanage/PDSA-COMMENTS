# Copy this file to .env.local for local frontend development.
# Next.js loads .env.local automatically (not committed to git).

# Backend API URL (Spring Boot server)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080